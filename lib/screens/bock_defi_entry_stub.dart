import 'package:flutter/material.dart';

Future<void> defiInitializeTables() async {}

Widget defiApp() {
  return const Scaffold(
    body: Center(
      child: Text('De-Fi is available on mobile only.'),
    ),
  );
}
